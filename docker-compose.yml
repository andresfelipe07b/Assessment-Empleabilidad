services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:6543/postgres
      - SPRING_DATASOURCE_USERNAME=postgres.lssqoyjaenhayhbxjujb
      - SPRING_DATASOURCE_PASSWORD=dCW3HRNKak60d0mg
    restart: always
